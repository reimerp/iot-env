# for debug output to file(s) or stdout
# on logs be careful: self feeding
[[outputs.file]]
  files = ["stdout"]
  # use_batch_format = false
  data_format = "influx"
  ## Output string fields as metric labels; when false string fields are discarded.
  #prometheus_string_as_label = true
  #data_format = "prometheus"

